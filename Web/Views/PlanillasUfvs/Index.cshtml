@model IEnumerable<Cerberus.Sos.Accounting.BusinessLogic.Entities.ParametrosPlanillas.RegistroUfv>

@{
    ViewBag.Title = "Registro UFV's";
}

<h1 class="page-header">Registro UFV's</h1>

<p>
    @Html.ActionLink("Agregar", "Create")
</p>
@if (Model.Any())
{
    <div class="table-responsive">
        <table class="table table-striped table-bordered table-hover">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Fecha</th>
                    <th>UFV Anterior</th>
                    <th>UFV Actual</th>
                    <th>Acciones</th>
                </tr>
            </thead>

            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.ID</td>
                        <td>@item.FECHA_UFV</td>
                        <td>@item.UFV_ANTERIOR</td>
                        <td>@item.UFV_ACTUAL</td>
                        <td>
                            @Html.ActionLink("Modificar", "Edit", new { id = item.ID }) |
                            @Html.ActionLink("Eliminar", "Delete", new { id = item.ID }, new { @onclick = "return confirm('Esta seguro de borrar el registro?');"})
                        </td>
                    </tr>
                }
            </tbody>

        </table>
    </div>

}